spring.application.name=parsaberpro

# Configuración del servidor (Render asigna el puerto automáticamente)
server.port=${PORT:8080}

# MongoDB Atlas Configuration
# En Render, se usa la variable de entorno MONGODB_URI
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://jfelipepabong_db_user:Pabon1705@cluster0.msnxvio.mongodb.net/parsaberpro_db}
spring.data.mongodb.database=parsaberpro_db

# DevTools (deshabilitado en producción)
spring.devtools.restart.enabled=false

# Configuración adicional (opcional)
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
